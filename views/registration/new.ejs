<h2>New User</h2>

<form action="/registration" method="POST">
  <% Object.keys(template).forEach(key => { %>
    <div class="field">
      <label class="label"><%= key.toUpperCase()%></label>
      <div class="control">
      <% if (key === 'profile') { %>
        <% Object.keys(template.profile).forEach((element) => { %>
          <div class="field is-horizontal">
          <div class="field-label is-normal">
          <label class="label"><%= element.toUpperCase() %></label>
          </div>
          <% if (element === 'firstName' || element === 'secondName' || element === 'image') { %>

            <div class="field-body">
              <div class="field">
                <div class="control">
                  <input type="text" class="input" name="<%=key%>.<%= element %>">
                </div>
              </div>
            </div>
          </div>

          <% } else if (element === 'DOB') { %>

            <div class="field-body">
              <div class="field">
                <div class="control">
                  <input type="date" class="input" name="<%=key %>.<%= element %>">
                </div>
              </div>
            </div>
          </div>

          <% } else { %>

            <div class="field-body">
              <div class="field">
                <div class="control">
                  <textarea name="<%=key %>.<%= element %>" rows="10" columns="80"></textarea>
                </div>
              </div>
            </div>
          </div>

          <% }%>
        <% }) %>
      </div>
    </div>
  <% } else if(key === 'password') { %>
    <input type="password" class="input" name="<%= key %>">
    <label class="label">PASSWORD CONFIRMATION</label>
    <input type="password" class="input" name="passwordConfirmation">
    <% } else { %>
      <input type="text" class="input" name="<%= key %>">
  <% } %>
  <% }) %>
  <div class="field is-grouped">
    <div class="control">
      <button class="button is-link">Submit</button>
    </div>
    <div class="control">
      <button class="button is-text">Cancel</button>
    </div>
  </div>
</form>
